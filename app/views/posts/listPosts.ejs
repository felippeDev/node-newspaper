<!DOCTYPE html>
<html lang="en-US">
    <head>
        <meta charset="utf-8">
        <title>All Post</title>
    </head>
    <body>
        <h1>All Posts</h1>
        <table>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
            </tr>

            <% for(let i = 0; i < posts.length; i++) { %>
                <tr>
                    <td><%= posts[i].id %></td>
                    <td><%= posts[i].title %></td>
                    <td><%= posts[i].content %></td>
                </tr>
            <% } %>
        </table>
    </body>
</html>